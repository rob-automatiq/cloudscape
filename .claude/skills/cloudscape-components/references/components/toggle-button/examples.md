# toggle-button playground examples

Snapshot of the named examples on https://cloudscape.design/components/toggle-button/?tabId=playground (taken 2026-09-24).
The playground stores every value as text: treat a value as code when the prop's type in `api.md` is a function, React node, object, or array, and as a plain string when the type is `string`.
Combine an example's props with the base setup to reproduce it; the wrapper shows the markup the example is placed in.

## Base setup


## Examples

### Normal toggle button

Playground id: `normal-toggle-button`

- `children`: `Favorite`
- `iconName`: `star`
- `pressedIconName`: `star-filled`

### Normal toggle button without label

Playground id: `normal-toggle-button-without-label`

- `ariaLabel`: `Favorite`
- `iconName`: `star`
- `pressedIconName`: `star-filled`

### Icon toggle button

Playground id: `icon-toggle-button`

- `variant`: `icon`
- `ariaLabel`: `Favorite`
- `iconName`: `star`
- `pressedIconName`: `star-filled`
