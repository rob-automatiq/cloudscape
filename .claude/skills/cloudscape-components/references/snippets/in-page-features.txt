import React, { useState } from 'react';

import { ColumnLayout, Header, Container, SpaceBetween, Link, Box, Input, FormField } from '@cloudscape-design/components';
export default function InPageFeatures() {
  const [bucketValue, setBucketValue] = useState('');
  const [regionValue, setRegionValue] = useState('');
  return (
    <SpaceBetween size="l">
      <Container header={<Header variant="h2">Distribution content delivery</Header>}>
        <SpaceBetween size="m">
          <FormField label="Bucket">
            <Input value={bucketValue} onChange={event => setBucketValue(event.detail.value)} />
          </FormField>
          <FormField
            label={
              <>
                Region - <em>new</em>
              </>
            }
          >
            <Input value={regionValue} onChange={event => setRegionValue(event.detail.value)} />
          </FormField>
        </SpaceBetween>
      </Container>
      <Container
        header={
          <Header variant="h2" info={<Link variant="info">Info</Link>}>
            Service overview - <em>new</em>
          </Header>
        }
      >
        <SpaceBetween size="l">
          <ColumnLayout columns={4} variant="text-grid" minColumnWidth={170}>
            <div>
              <Box variant="awsui-key-label">Running instances</Box>
              <Link variant="awsui-value-large" href="#" ariaLabel="Running instances (14)">
                14
              </Link>
            </div>
            <div>
              <Box variant="awsui-key-label">Volumes</Box>
              <Link variant="awsui-value-large" href="#" ariaLabel="Volumes (126)">
                126
              </Link>
            </div>
            <div>
              <Box variant="awsui-key-label">Security groups</Box>
              <Link variant="awsui-value-large" href="#" ariaLabel="Security groups (116)">
                116
              </Link>
            </div>
            <div>
              <Box variant="awsui-key-label">Load balancers</Box>
              <Link variant="awsui-value-large" href="#" ariaLabel="Load balancers (28)">
                28
              </Link>
            </div>
          </ColumnLayout>
        </SpaceBetween>
      </Container>
    </SpaceBetween>
  );
}
