/* eslint-disable react/no-unescaped-entities */
import React from 'react';
import { Alert, ExpandableSection, Link } from '@cloudscape-design/components';

const Snippet = () => {
  return (
    <Alert type="error" statusIconAriaLabel="Error" header="There was an error">
      <p>
        Reload the page or try again later. <Link href="#">Contact us</Link> and share more details.
      </p>
      <ExpandableSection headerText="Error details">
        Cannot set properties of null (setting 'innerHTML')
      </ExpandableSection>
    </Alert>
  );
};

export default Snippet;
